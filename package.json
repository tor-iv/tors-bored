{
  "name": "tors-bored",
  "private": true,
  "workspaces": [
    "pottery-auction"
  ],
  "scripts": {
    "dev": "bun run --filter marketplace dev",
    "build": "bun run --filter marketplace build",
    "start": "bun run --filter marketplace start",
    "lint": "bun run --filter marketplace lint",
    "test": "bun run --filter marketplace test"
  },
  "devDependencies": {},
  "dependencies": {}
}